services:
  shidenryu-lp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shidenryu-lp
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
